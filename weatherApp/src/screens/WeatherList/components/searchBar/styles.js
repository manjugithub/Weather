import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    sectionContainer: {
        marginTop: 10,
        height: 44,
        marginHorizontal: 5,
        borderColor: 'black',
        borderWidth: 1,
        borderRadius: 10,
        alignItems: 'center',
        justifyContent: 'center',
        flexDirection: 'row'
    },
    textField: {
        width: '80%',
        height: '100%',
        marginRight: '5%',
    },
    clearButton: {
        width: '10%',
        height: '100%',
        alignItems: 'center',
        justifyContent: 'center',

    },
});
